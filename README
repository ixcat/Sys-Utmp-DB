
Sys-Utmp-DB: utmp(5)/wtmp(5) high-level parser & SQLite DB

Requires Sys::Utmp and DBD::SQLite.

Interprets Sys::Utmp records (e.g. synthesizes login records from raw data)
and loads into a simple SQLite database table.

installation:

  perl Makefile.PL
  make
  make install

usage:

  utmpdb : actual application; run with no arguments for synopsys
  utmpdmp : low-level Sys::Utmp dump
  utmpproc : high-level synthetic record dump

